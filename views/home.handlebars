{{!-- Jumbotron Fluid --}}
<div class="jumbotron">
    <div class="container text-center">
        <h1 class="display-4">Rotten Tomatoes Scraper</h1>
        <p class="lead">Scrape articles from Rotten Tomatoes, save your favorite and make notes!</p>
            {{!-- Button Row --}}
        <div class="row justify-content-center" id="btn-row">
            <div class="col-6 text-center d-flex justify-content-around" id="btn-col">
                <button type="button"  class="scrapeBtn btn btn-lg btn-light">Scrape Articles</button>
                <button type="button" class="clearBtn btn btn-lg btn-light">Clear Articles</button>
            </div>
        </div>
    </div>
</div>

{{!-- Main Body  --}}
<div class="container-fluid main-cont" >

    {{!-- Articles/Notes Section --}}
    <div class="row" id="article-row">
        <div class="col-lg-12 articles" id="articleContainer">
            {{#each article}}
            <div class="card article-card" data-_id="{{ this._id }}" data-fav="{{ this.favorite }}">
                <img class="card-img-top" src="{{ this.image }}" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">{{ this.title }}</h5>
                    <p class="card-text">{{ this.publication }}</p>
                    <button href="{{ this.link }}" class="btn btn-sm btn-outline-secondary">Read on RT</button>
                    <button data-title="{{ this.title }}"
                        class="btn btn-sm btn-outline-secondary noteBtn" data-toggle="modal"
                        data-target="#notes-modal">Add a Note</button>
                    <button href="#"class="btn btn-sm btn-outline-secondary favBtn">
                        <span data-_id={{this._id}} class="heartIcon heart-{{ this.favorite }}">
                            <i class="fas fa-heart"></i>
                        </span>
                    </button>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    {{!-- New Note Modal --}}
    <div id="notes-modal" class="modal fade bd-note-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel"
        aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content">
                <div class="row justify-content-center" id="note-row">
                    <div id="note-col" class="col-9">
                        <div class="modal-body">
                            <h2 id="note-heading"></h2>
                            <form id="add-note-form">
                                <div class="form-group">
                                    <label for="note-text-area">Write your Note Here...</label>
                                    <textarea class="form-control" id="note-text-area" rows="5"></textarea>
                                </div>
                                <button id="submitNoteBtn" type="button" class="btn btn-primary">Add Note</button>
                            </form>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
</div>


<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
{{!-- {{!-- <script src="https://cdnjs.cloudflare.com/ajax/libs/bootbox.js/4.4.0/bootbox.min.js"></script> --}} --}}
<script src="/assets/js/home.js"></script>